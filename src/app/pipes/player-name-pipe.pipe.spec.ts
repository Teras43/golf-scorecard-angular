import { PlayerNamePipePipe } from './player-name-pipe.pipe';

describe('PlayerNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
